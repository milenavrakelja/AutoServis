<UserControl x:Class="AutoServis.ClientApp.Views.RacuniView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Text="{DynamicResource Racuni_Title}" 
                   FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <DatePicker x:Name="DatePicker" 
                        SelectedDate="{Binding Datum}" 
                        Margin="0,0,10,0"
                        Background="{DynamicResource BackgroundBrush}"
                        Foreground="{DynamicResource ForegroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"/>

            <Button Content="{DynamicResource Racuni_PretraziButton}" 
                    Click="PretraziBtn_Click" Width="160"/>

            <Button Content="{DynamicResource Racuni_PrikaziSveButton}" 
                    Click="PrikaziSveBtn_Click" Width="100" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Container za DataGrid i poruku -->
        <Grid Grid.Row="2">
            <DataGrid x:Name="RacuniGrid"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      ItemsSource="{Binding Racuni}"
                      Background="{DynamicResource BackgroundBrush}"
                      Foreground="{DynamicResource ForegroundBrush}"
                      BorderBrush="{DynamicResource BorderBrush}"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Racuni_BrojRacunaHeader}" 
                                        Binding="{Binding BrojRacuna}" Width="120"/>
                    <DataGridTextColumn Header="{DynamicResource Racuni_DatumHeader}" 
                                        Binding="{Binding DatumIzdavanja}" Width="120"/>
                    <DataGridTextColumn Header="{DynamicResource Racuni_UkupnoHeader}" 
                                        Binding="{Binding UkupnaCijena, StringFormat='{}{0:N2} KM'}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Nema računa za prikaz."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>