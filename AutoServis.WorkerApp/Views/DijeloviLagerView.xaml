<UserControl x:Class="AutoServis.WorkerApp.Views.DijeloviLagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Text="{DynamicResource Worker_Dijelovi_Title}" 
                   FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Forma za dodavanje -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="NazivBox" Width="250" Margin="0,0,10,0"/>
            <TextBox x:Name="KolicinaBox" Width="100" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Worker_Dijelovi_DodajObnoviButton}" 
                    Click="DodajBtn_Click" Width="120"/>
        </StackPanel>

        <!-- Container za DataGrid i poruku -->
        <Grid Grid.Row="2">
            <DataGrid x:Name="DijeloviGrid"
                      AutoGenerateColumns="False" IsReadOnly="False"
                      CanUserAddRows="False" SelectionMode="Single"
                      ItemsSource="{Binding Dijelovi}"
                      Background="{DynamicResource BackgroundBrush}"
                      Foreground="{DynamicResource ForegroundBrush}"
                      BorderBrush="{DynamicResource BorderBrush}">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Worker_Dijelovi_NazivHeader}" 
                                        Binding="{Binding Naziv}" Width="250"/>
                    <DataGridTextColumn Header="{DynamicResource Worker_Dijelovi_KolicinaHeader}" 
                                        Binding="{Binding KolicinaNaLageru}" Width="120"/>
                    <DataGridTemplateColumn Header="{DynamicResource Worker_Dijelovi_UvecajButton}" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{DynamicResource Worker_Dijelovi_UvecajButton}" 
                                        Click="UvecajBtn_Click" 
                                        Tag="{Binding}" Margin="2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Nema dijelova u lageru."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>