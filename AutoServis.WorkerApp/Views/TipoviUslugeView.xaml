<UserControl x:Class="AutoServis.WorkerApp.Views.TipoviUslugeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource Worker_Tipovi_Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Forma za dodavanje -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="NazivBox" Width="200" Height="30" Margin="0,0,10,0" materialDesign:HintAssist.Hint="{DynamicResource Worker_Tipovi_NazivUslugeHint}"/>
            <TextBox x:Name="CijenaBox" Width="100" Height="30" Margin="0,0,10,0" materialDesign:HintAssist.Hint="{DynamicResource Worker_Tipovi_CijenaHint}"/>
            <Button Content="{DynamicResource Worker_Tipovi_DodajButton}" Click="DodajBtn_Click" Width="80" Height="35"/>
        </StackPanel>

        

        <!-- Container za DataGrid i poruku -->
        <Grid Grid.Row="3">
            <DataGrid x:Name="TipoviGrid"
                      AutoGenerateColumns="False" IsReadOnly="False"
                      CanUserAddRows="False" SelectionMode="Single"
                      ItemsSource="{Binding Tipovi}">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Worker_Tipovi_NazivHeader}" Binding="{Binding Naziv}" Width="200"/>
                    <DataGridTextColumn Header="{DynamicResource Worker_Tipovi_CijenaHeader}" 
                                        Binding="{Binding Cijena, StringFormat='{}{0:N2} KM'}" 
                                        Width="120"/>
                    <DataGridTemplateColumn Header="{DynamicResource Worker_Tipovi_AkcijeHeader}" Width="300">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{DynamicResource Worker_Tipovi_IzmeniCenuButton}" Click="IzmeniCenuBtn_Click" Tag="{Binding}" Margin="2"/>
                                    <Button Content="{DynamicResource Worker_Tipovi_ObrisiButton}" Click="ObrisiBtn_Click" Tag="{Binding}" Margin="2" Background="Red" Foreground="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Nema tipova usluga za prikaz."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>