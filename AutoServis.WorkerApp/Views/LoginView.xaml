<UserControl x:Class="AutoServis.WorkerApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="40">
        <StackPanel HorizontalAlignment="Center" Width="300">
            <!-- Naslov -->
            <TextBlock Text="{DynamicResource Worker_Login_Title}" 
                       FontWeight="Bold" Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Izbor jezika -->
            <ComboBox SelectedIndex="0" SelectionChanged="LanguageComboBox_SelectionChanged" 
                      Margin="0,0,0,20" Width="150" HorizontalAlignment="Right">
                <ComboBoxItem Content="Srpski (sr)" Tag="sr"/>
                <ComboBoxItem Content="English (en)" Tag="en"/>
            </ComboBox>

            <!-- Korisničko ime sa hint-om -->
            <Grid Margin="0,0,0,10">
                <TextBox x:Name="UsernameBox"
                         Text="{Binding Username, Mode=TwoWay}"/>
                <TextBlock Text="{DynamicResource Worker_Login_UsernameHint}"
                           Foreground="#AAAAAA"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           Padding="10,8">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=UsernameBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Lozinka sa hint-om -->
            <Grid Margin="0,0,0,20">
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" />
                <TextBlock x:Name="PasswordHint"
                           Text="{DynamicResource Worker_Login_PasswordHint}"
                           Foreground="#AAAAAA"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           Padding="10,8" />
            </Grid>

            <!-- Dugme za prijavu -->
            <Button Content="{DynamicResource Worker_Login_Button}" 
                    Click="LoginBtn_Click" 
                    Margin="0,0,0,10"/>

            <!-- Link za registraciju (opciono za worker) -->
            <!-- Ako radnici ne mogu da se registruju, možeš ovo obrisati -->
            <Button Content="{DynamicResource Worker_Login_RegisterLink}" 
                    Click="RegisterBtn_Click" 
                    Background="Transparent" BorderBrush="Transparent"
                    HorizontalAlignment="Center" Padding="5">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   TextDecorations="Underline"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>